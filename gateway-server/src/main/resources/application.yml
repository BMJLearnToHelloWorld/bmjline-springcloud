server:
  port: 8090

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: blog_server
          uri: lb://blog-server
          predicates:
            - Path=/blog/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
