server:
  port: 8090

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: blog_server
          uri: lb://blog-server
          predicates:
            - Path=/blog/**
        - id: admin_server
          uri: lb://admin-server
          predicates:
            - Path=/admin/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
